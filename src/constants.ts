/**
 * App Constants — Docs2
 */

export const APP_NAME = 'docs2'

export const SCOPE_ID = `app:${APP_NAME}`

export { ROLES, ROLE_CONFIG, type Role, getUserColor, DEFAULT_USER_COLORS as CURSOR_COLORS } from 'deepspace'
